{
  "name": "piper",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ robin-thomas/piper",
    "create-local": "graph create --node http://localhost:8020/ robin-thomas/piper",
    "remove-local": "graph remove --node http://localhost:8020/ robin-thomas/piper",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 robin-thomas/piper"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.13.3",
    "@graphprotocol/graph-ts": "0.13.0"
  }
}
